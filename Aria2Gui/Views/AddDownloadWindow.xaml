<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="Aria2Gui.Views.AddDownloadWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:loc="using:Aria2Gui.Helpers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:vm="using:Aria2Gui.ViewModels"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer
            Grid.Row="0"
            Padding="24,20,24,12"
            VerticalScrollBarVisibility="Auto"
            HorizontalScrollMode="Disabled">
            <StackPanel Spacing="12">
                <TextBox
                    x:Name="UrlsBox"
                    loc:Localize.Uid="UrlsBox"
                    AutomationProperties.AutomationId="UrlsBox"
                    PlaceholderText="magnet:?xt=urn:btih:…"
                    AcceptsReturn="True"
                    TextWrapping="Wrap"
                    MinHeight="88"
                    ScrollViewer.VerticalScrollBarVisibility="Auto" />

                <!-- Save destination -->
                <Grid ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox
                        x:Name="DirBox"
                        loc:Localize.Uid="AddDirBox"
                        AutomationProperties.AutomationId="AddDirBox"
                        IsReadOnly="True" />
                    <Button
                        loc:Localize.Uid="AddBrowseFolderButton"
                        AutomationProperties.AutomationId="AddBrowseFolderButton"
                        Grid.Column="1"
                        VerticalAlignment="Bottom"
                        Click="OnBrowseFolderClick" />
                </Grid>

                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button
                        loc:Localize.Uid="PickTorrentButton"
                        AutomationProperties.AutomationId="PickTorrentButton"
                        Click="OnPickTorrentClick" />
                    <TextBlock
                        x:Name="TorrentFileName"
                        VerticalAlignment="Center"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        TextTrimming="CharacterEllipsis"
                        MaxWidth="280" />
                </StackPanel>

                <!-- Torrent contents (appears after picking a .torrent) -->
                <StackPanel x:Name="TorrentFilesPanel" Spacing="8" Visibility="Collapsed">
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            loc:Localize.Uid="TorrentFilesHeader"
                            Style="{StaticResource BodyStrongTextBlockStyle}"
                            VerticalAlignment="Center" />
                        <Button
                            loc:Localize.Uid="SelectAllFilesButton"
                            AutomationProperties.AutomationId="SelectAllFilesButton"
                            Grid.Column="1"
                            Click="OnSelectAllClick" />
                        <Button
                            loc:Localize.Uid="DeselectAllFilesButton"
                            AutomationProperties.AutomationId="DeselectAllFilesButton"
                            Grid.Column="2"
                            Click="OnDeselectAllClick" />
                    </Grid>
                    <Border
                        BorderThickness="1"
                        BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                        CornerRadius="{ThemeResource ControlCornerRadius}"
                        Height="248">
                        <!-- Long file names scroll horizontally instead of clipping. -->
                        <TreeView
                            x:Name="FileTree"
                            AutomationProperties.AutomationId="TorrentFileTree"
                            SelectionMode="Multiple"
                            ScrollViewer.HorizontalScrollBarVisibility="Auto"
                            ScrollViewer.HorizontalScrollMode="Enabled">
                            <!-- In unbound mode the template's data root is the TreeViewNode,
                                 whose Content carries our TorrentNodeContent. -->
                            <TreeView.ItemTemplate>
                                <DataTemplate x:DataType="muxc:TreeViewNode">
                                    <TextBlock Text="{x:Bind ((vm:TorrentNodeContent)Content).Label, Mode=OneTime}" />
                                </DataTemplate>
                            </TreeView.ItemTemplate>
                        </TreeView>
                    </Border>
                </StackPanel>

                <InfoBar
                    x:Name="ErrorBar"
                    Severity="Error"
                    IsOpen="False"
                    IsClosable="False" />
            </StackPanel>
        </ScrollViewer>

        <!-- Action bar: accent "Add" + standard "Cancel", like a dialog. -->
        <Grid
            Grid.Row="1"
            Padding="24,12,24,16"
            BorderThickness="0,1,0,0"
            BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                <Button
                    x:Name="AddButton"
                    loc:Localize.Uid="ConfirmAddButton"
                    AutomationProperties.AutomationId="ConfirmAddButton"
                    Style="{StaticResource AccentButtonStyle}"
                    MinWidth="104"
                    Click="OnAddClick" />
                <Button
                    loc:Localize.Uid="CancelAddButton"
                    AutomationProperties.AutomationId="CancelAddButton"
                    MinWidth="104"
                    Click="OnCancelClick">
                    <Button.KeyboardAccelerators>
                        <KeyboardAccelerator Key="Escape" />
                    </Button.KeyboardAccelerators>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
