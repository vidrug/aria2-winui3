<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Aria2Gui.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:tk="using:CommunityToolkit.WinUI.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- WinUI Gallery look: a flat back arrow in the top-left corner, page title
             in the content below. A standard Button (not NavigationBackButtonNormalStyle,
             whose hover highlight renders square in this SDK) gives a rounded hover via
             the default template's ControlCornerRadius and a reliable direct Click. -->
        <Button
            Grid.Row="0"
            x:Uid="SettingsBackButton"
            AutomationProperties.AutomationId="SettingsBackButton"
            Width="40"
            Height="40"
            Padding="0"
            Margin="4,4,0,0"
            Background="Transparent"
            BorderThickness="0"
            Click="OnBackClick">
            <FontIcon Glyph="&#xE72B;" FontSize="16" />
        </Button>

        <NavigationView
            x:Name="SettingsNav"
            Grid.Row="1"
            PaneDisplayMode="Left"
            IsSettingsVisible="False"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleButtonVisible="False"
            OpenPaneLength="240"
            SelectionChanged="OnSectionChanged">
            <NavigationView.Resources>
                <!-- Transparent surfaces so the window's Mica backdrop shows through,
                     matching the rest of the app instead of a flat grey panel. Scoped
                     to Light/Dark only — in High Contrast (Mica is off) an empty HC
                     dictionary keeps the opaque system pane fill and visible divider. -->
                <ResourceDictionary>
                    <ResourceDictionary.ThemeDictionaries>
                        <ResourceDictionary x:Key="Light">
                            <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent" />
                            <SolidColorBrush x:Key="NavigationViewDefaultPaneBackground" Color="Transparent" />
                            <SolidColorBrush x:Key="NavigationViewExpandedPaneBackground" Color="Transparent" />
                            <SolidColorBrush x:Key="NavigationViewContentGridBorderBrush" Color="Transparent" />
                            <Thickness x:Key="NavigationViewContentGridBorderThickness">0</Thickness>
                        </ResourceDictionary>
                        <ResourceDictionary x:Key="Dark">
                            <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent" />
                            <SolidColorBrush x:Key="NavigationViewDefaultPaneBackground" Color="Transparent" />
                            <SolidColorBrush x:Key="NavigationViewExpandedPaneBackground" Color="Transparent" />
                            <SolidColorBrush x:Key="NavigationViewContentGridBorderBrush" Color="Transparent" />
                            <Thickness x:Key="NavigationViewContentGridBorderThickness">0</Thickness>
                        </ResourceDictionary>
                        <ResourceDictionary x:Key="HighContrast" />
                    </ResourceDictionary.ThemeDictionaries>
                    <CornerRadius x:Key="NavigationViewContentGridCornerRadius">0</CornerRadius>
                </ResourceDictionary>
            </NavigationView.Resources>
            <NavigationView.MenuItems>
                <NavigationViewItem x:Uid="NavGeneral" Tag="general" IsSelected="True" AutomationProperties.AutomationId="NavGeneral">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE713;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="NavConnection" Tag="connection" AutomationProperties.AutomationId="NavConnection">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE968;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="NavFiles" Tag="files" AutomationProperties.AutomationId="NavFiles">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEDA2;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="BitTorrent" Tag="bt" AutomationProperties.AutomationId="NavBt">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE896;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="NavAdvanced" Tag="advanced" AutomationProperties.AutomationId="NavAdvanced">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE712;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <ScrollViewer
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollBarVisibility="Disabled"
                HorizontalScrollMode="Disabled"
                Padding="24,8,24,16">
                <!-- Grid gives the panel the full content width so the cards stretch
                     to MaxWidth and centre (a Center StackPanel would shrink to fit). -->
                <Grid HorizontalAlignment="Stretch">
                <StackPanel MaxWidth="1000" HorizontalAlignment="Stretch">

                    <TextBlock x:Uid="SettingsTitle" Style="{StaticResource TitleTextBlockStyle}" Margin="0,8,0,20" />

                    <!-- ===== Основные ===== -->
                    <StackPanel x:Name="GeneralPanel" Spacing="4">
                        <tk:SettingsCard x:Uid="DownloadFolderCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE838;" />
                            </tk:SettingsCard.HeaderIcon>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock x:Name="DirText" AutomationProperties.AutomationId="DownloadDirText" VerticalAlignment="Center" MaxWidth="280" TextTrimming="CharacterEllipsis" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <Button x:Uid="BrowseFolderButton" AutomationProperties.AutomationId="BrowseFolderButton" Click="OnBrowseClick" />
                            </StackPanel>
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="DownLimitCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE896;" />
                            </tk:SettingsCard.HeaderIcon>
                            <NumberBox x:Name="DownLimitBox" AutomationProperties.AutomationId="DownLimitBox" Minimum="0" Maximum="10000" SmallChange="0.5" MinWidth="148" SpinButtonPlacementMode="Compact" />
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="UpLimitCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE898;" />
                            </tk:SettingsCard.HeaderIcon>
                            <NumberBox x:Name="UpLimitBox" AutomationProperties.AutomationId="UpLimitBox" Minimum="0" Maximum="10000" SmallChange="0.5" MinWidth="148" SpinButtonPlacementMode="Compact" />
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="ConcurrentCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE71D;" />
                            </tk:SettingsCard.HeaderIcon>
                            <NumberBox x:Name="ConcurrentBox" AutomationProperties.AutomationId="ConcurrentBox" Minimum="1" Maximum="50" MinWidth="148" SpinButtonPlacementMode="Compact" />
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="ConnectionsCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE968;" />
                            </tk:SettingsCard.HeaderIcon>
                            <NumberBox x:Name="ConnectionsBox" AutomationProperties.AutomationId="ConnectionsBox" Minimum="1" Maximum="16" MinWidth="148" SpinButtonPlacementMode="Compact" />
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="ThemeCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE790;" />
                            </tk:SettingsCard.HeaderIcon>
                            <ComboBox x:Name="ThemeBox" AutomationProperties.AutomationId="ThemeBox" MinWidth="180">
                                <ComboBoxItem x:Uid="ThemeSystemItem" Tag="Default" />
                                <ComboBoxItem x:Uid="ThemeLightItem" Tag="Light" />
                                <ComboBoxItem x:Uid="ThemeDarkItem" Tag="Dark" />
                            </ComboBox>
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="LanguageCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE774;" />
                            </tk:SettingsCard.HeaderIcon>
                            <!-- Language names are autonyms (shown the same regardless of UI language). -->
                            <ComboBox x:Name="LanguageBox" AutomationProperties.AutomationId="LanguageBox" MinWidth="200">
                                <ComboBoxItem x:Uid="LanguageSystemItem" Tag="" />
                                <ComboBoxItem Content="English" Tag="en-US" />
                                <ComboBoxItem Content="Русский" Tag="ru" />
                                <ComboBoxItem Content="Español" Tag="es" />
                                <ComboBoxItem Content="Deutsch" Tag="de" />
                                <ComboBoxItem Content="Français" Tag="fr" />
                                <ComboBoxItem Content="Português (Brasil)" Tag="pt-BR" />
                                <ComboBoxItem Content="Italiano" Tag="it" />
                                <ComboBoxItem Content="中文（简体）" Tag="zh-Hans" />
                                <ComboBoxItem Content="日本語" Tag="ja" />
                                <ComboBoxItem Content="Українська" Tag="uk" />
                                <ComboBoxItem Content="Polski" Tag="pl" />
                                <ComboBoxItem Content="Türkçe" Tag="tr" />
                            </ComboBox>
                        </tk:SettingsCard>
                    </StackPanel>

                    <!-- ===== Подключение ===== -->
                    <StackPanel x:Name="ConnectionPanel" Spacing="4" Visibility="Collapsed">
                        <tk:SettingsCard x:Uid="ProxyCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE968;" />
                            </tk:SettingsCard.HeaderIcon>
                            <TextBox x:Name="ProxyBox" x:Uid="ProxyBox" AutomationProperties.AutomationId="ProxyBox" MinWidth="300" />
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="CheckCertCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE72E;" />
                            </tk:SettingsCard.HeaderIcon>
                            <ToggleSwitch x:Name="CheckCertToggle" x:Uid="CheckCertToggle" AutomationProperties.AutomationId="CheckCertToggle" />
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="TimeoutCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE916;" />
                            </tk:SettingsCard.HeaderIcon>
                            <NumberBox x:Name="TimeoutBox" AutomationProperties.AutomationId="TimeoutBox" Minimum="1" Maximum="600" MinWidth="148" SpinButtonPlacementMode="Compact" />
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="ConnectTimeoutCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE839;" />
                            </tk:SettingsCard.HeaderIcon>
                            <NumberBox x:Name="ConnectTimeoutBox" AutomationProperties.AutomationId="ConnectTimeoutBox" Minimum="1" Maximum="600" MinWidth="148" SpinButtonPlacementMode="Compact" />
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="MaxTriesCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE72C;" />
                            </tk:SettingsCard.HeaderIcon>
                            <NumberBox x:Name="MaxTriesBox" AutomationProperties.AutomationId="MaxTriesBox" Minimum="0" Maximum="100" MinWidth="148" SpinButtonPlacementMode="Compact" />
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="RetryWaitCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE823;" />
                            </tk:SettingsCard.HeaderIcon>
                            <NumberBox x:Name="RetryWaitBox" AutomationProperties.AutomationId="RetryWaitBox" Minimum="0" Maximum="600" MinWidth="148" SpinButtonPlacementMode="Compact" />
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="MinSplitSizeCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE8B7;" />
                            </tk:SettingsCard.HeaderIcon>
                            <TextBox x:Name="MinSplitSizeBox" AutomationProperties.AutomationId="MinSplitSizeBox" MinWidth="148" PlaceholderText="20M" />
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="UserAgentCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE7C1;" />
                            </tk:SettingsCard.HeaderIcon>
                            <TextBox x:Name="UserAgentBox" x:Uid="UserAgentBox" AutomationProperties.AutomationId="UserAgentBox" MinWidth="300" />
                        </tk:SettingsCard>
                    </StackPanel>

                    <!-- ===== Файлы ===== -->
                    <StackPanel x:Name="FilesPanel" Spacing="4" Visibility="Collapsed">
                        <tk:SettingsCard x:Uid="FileAllocCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xEDA2;" />
                            </tk:SettingsCard.HeaderIcon>
                            <ComboBox x:Name="FileAllocBox" AutomationProperties.AutomationId="FileAllocBox" MinWidth="180">
                                <ComboBoxItem x:Uid="FileAllocAutoItem" Tag="auto" />
                                <ComboBoxItem x:Uid="FileAllocNoneItem" Tag="none" />
                                <ComboBoxItem x:Uid="FileAllocPreallocItem" Tag="prealloc" />
                                <ComboBoxItem x:Uid="FileAllocTruncItem" Tag="trunc" />
                                <ComboBoxItem x:Uid="FileAllocFallocItem" Tag="falloc" />
                            </ComboBox>
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="AllowOverwriteCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE74D;" />
                            </tk:SettingsCard.HeaderIcon>
                            <ToggleSwitch x:Name="AllowOverwriteToggle" x:Uid="AllowOverwriteToggle" AutomationProperties.AutomationId="AllowOverwriteToggle" />
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="AutoRenameCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE8AC;" />
                            </tk:SettingsCard.HeaderIcon>
                            <ToggleSwitch x:Name="AutoRenameToggle" x:Uid="AutoRenameToggle" AutomationProperties.AutomationId="AutoRenameToggle" />
                        </tk:SettingsCard>
                    </StackPanel>

                    <!-- ===== BitTorrent ===== -->
                    <StackPanel x:Name="BtPanel" Spacing="4" Visibility="Collapsed">
                        <tk:SettingsCard x:Uid="ListenPortCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE945;" />
                            </tk:SettingsCard.HeaderIcon>
                            <NumberBox x:Name="ListenPortBox" AutomationProperties.AutomationId="ListenPortBox" Minimum="0" Maximum="65535" MinWidth="148" SpinButtonPlacementMode="Compact" />
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="DhtCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE71B;" />
                            </tk:SettingsCard.HeaderIcon>
                            <ToggleSwitch x:Name="DhtToggle" x:Uid="DhtToggle" AutomationProperties.AutomationId="DhtToggle" />
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="PexCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE8AF;" />
                            </tk:SettingsCard.HeaderIcon>
                            <ToggleSwitch x:Name="PexToggle" x:Uid="PexToggle" AutomationProperties.AutomationId="PexToggle" />
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="LpdCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE839;" />
                            </tk:SettingsCard.HeaderIcon>
                            <ToggleSwitch x:Name="LpdToggle" x:Uid="LpdToggle" AutomationProperties.AutomationId="LpdToggle" />
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="CryptoCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE72E;" />
                            </tk:SettingsCard.HeaderIcon>
                            <ToggleSwitch x:Name="CryptoToggle" x:Uid="CryptoToggle" AutomationProperties.AutomationId="CryptoToggle" Toggled="OnCryptoToggled" />
                        </tk:SettingsCard>
                        <!-- Shown only when encryption is required: picks bt-min-crypto-level. -->
                        <tk:SettingsCard x:Name="CryptoLevelCard" x:Uid="CryptoLevelCard" ContentAlignment="Vertical" HorizontalContentAlignment="Stretch" Visibility="Collapsed">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE8D7;" />
                            </tk:SettingsCard.HeaderIcon>
                            <RadioButtons x:Name="CryptoLevelRadio" AutomationProperties.AutomationId="CryptoLevelRadio" HorizontalAlignment="Left">
                                <RadioButton AutomationProperties.AutomationId="CryptoLevelPlain">
                                    <StackPanel Spacing="2">
                                        <TextBlock x:Uid="CryptoLevelPlainTitle" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                        <TextBlock
                                            x:Uid="CryptoLevelPlainDesc"
                                            Style="{StaticResource CaptionTextBlockStyle}"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            TextWrapping="Wrap"
                                            MaxWidth="560"
                                            HorizontalAlignment="Left" />
                                    </StackPanel>
                                </RadioButton>
                                <RadioButton AutomationProperties.AutomationId="CryptoLevelArc4">
                                    <StackPanel Spacing="2">
                                        <TextBlock x:Uid="CryptoLevelArc4Title" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                        <TextBlock
                                            x:Uid="CryptoLevelArc4Desc"
                                            Style="{StaticResource CaptionTextBlockStyle}"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            TextWrapping="Wrap"
                                            MaxWidth="560"
                                            HorizontalAlignment="Left" />
                                    </StackPanel>
                                </RadioButton>
                            </RadioButtons>
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="MaxPeersCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE716;" />
                            </tk:SettingsCard.HeaderIcon>
                            <NumberBox x:Name="PeersBox" AutomationProperties.AutomationId="PeersBox" Minimum="0" Maximum="1000" MinWidth="148" SpinButtonPlacementMode="Compact" />
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="BtMaxOpenFilesCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE8B7;" />
                            </tk:SettingsCard.HeaderIcon>
                            <NumberBox x:Name="BtMaxOpenFilesBox" AutomationProperties.AutomationId="BtMaxOpenFilesBox" Minimum="1" Maximum="10000" MinWidth="148" SpinButtonPlacementMode="Compact" />
                        </tk:SettingsCard>
                        <tk:SettingsCard x:Uid="SeedRatioCard">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE898;" />
                            </tk:SettingsCard.HeaderIcon>
                            <NumberBox x:Name="SeedRatioBox" AutomationProperties.AutomationId="SeedRatioBox" Minimum="0" Maximum="100" SmallChange="0.1" MinWidth="148" SpinButtonPlacementMode="Compact" />
                        </tk:SettingsCard>
                        <tk:SettingsExpander x:Uid="TrackersExpander">
                            <tk:SettingsExpander.HeaderIcon>
                                <FontIcon Glyph="&#xE774;" />
                            </tk:SettingsExpander.HeaderIcon>
                            <tk:SettingsExpander.Items>
                                <tk:SettingsCard ContentAlignment="Vertical">
                                    <TextBox x:Name="TrackersBox" x:Uid="TrackersBox" AutomationProperties.AutomationId="TrackersBox" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="96" ScrollViewer.VerticalScrollBarVisibility="Auto" />
                                </tk:SettingsCard>
                            </tk:SettingsExpander.Items>
                        </tk:SettingsExpander>
                        <InfoBar x:Uid="BtRestartInfoBar" Severity="Informational" IsOpen="True" IsClosable="False" Margin="0,8,0,0" />
                    </StackPanel>

                    <!-- ===== Дополнительно ===== -->
                    <StackPanel x:Name="AdvancedPanel" Spacing="4" Visibility="Collapsed">
                        <tk:SettingsCard x:Uid="ExtraOptionsCard" ContentAlignment="Vertical">
                            <tk:SettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE713;" />
                            </tk:SettingsCard.HeaderIcon>
                            <TextBox x:Name="ExtraOptionsBox" AutomationProperties.AutomationId="ExtraOptionsBox" PlaceholderText="bt-request-peer-speed-limit=100K&#x0a;bt-stop-timeout=0" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="160" FontFamily="Consolas" ScrollViewer.VerticalScrollBarVisibility="Auto" />
                        </tk:SettingsCard>
                        <InfoBar x:Uid="AdvancedInfoBar" Severity="Informational" IsOpen="True" IsClosable="False" Margin="0,8,0,0" />
                    </StackPanel>

                    <InfoBar x:Name="ErrorBar" Severity="Error" IsOpen="False" IsClosable="False" Margin="0,8,0,16" />
                </StackPanel>
                </Grid>
            </ScrollViewer>
        </NavigationView>
    </Grid>
</UserControl>
